<div class="bg">
   <div class="box">
      <div class="box-inner">
         <%= form_with model: @user, url: user_session_path, local: true do |f| %>
         <% flash.each do |message_type, message| %>
