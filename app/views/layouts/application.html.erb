<!DOCTYPE html>
<html>
  <head>
    <title>VolunteerTree</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag('favicon.ico') %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header>
        <div>
          <%= link_to root_path, class: "text-white nav-link" do %>
            volunteer tree
          <% end %>
        </div>
        <div><%= link_to "トップ", root_path, class: "text-white nav-link" %></div>
        <div><%= link_to "記録する", new_record_path, class: "text-white nav-link" %></div>
        <div><%= link_to "記録一覧", records_path, class: "text-white nav-link" %></div>
        <div><%= link_to "下書き一覧", confirm_records_path, class: "text-white nav-link" %></div>
        <div><%= link_to "ユーザ一覧", users_path, class: "text-white nav-link" %></div>
        <div><%= link_to "マイページ", user_path(current_user.id), class: "text-white nav-link" %></div>
        <div><%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-white nav-link", form: { style: "display:inline" } %></div>
      </header>
    <% end %>   
    <%= yield %>
  </body>
</html>