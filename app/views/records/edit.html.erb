<h1>編集する</h1>
<%= form_with model:@record, local:true do |f| %>
    <h4>イベント名</h4>
    <%= f.text_field :event_name %>
    <h4>活動記録</h4>
    <%= f.text_area :content %><br>
    <%= f.submit '保存' %>
<% end %>